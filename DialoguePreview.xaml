<Window x:Class="HedgeHex.DialoguePreview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HedgeHex"
        mc:Ignorable="d"
        Title="Text Preview" Height="310" Width="388" MinWidth="388" MinHeight="310" Foreground="{x:Null}" ResizeMode="CanResize" Closing="Window_Closing">
    <Window.Resources>
        <FontFamily x:Key="font" >/fonts/#FOT-Seurat Pro M</FontFamily>
    </Window.Resources>
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FFB74343"/>
            <GradientStop Color="#FFEACA39" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid>

        <TextBlock x:Name="Text1" FontSize="25" 
               FontFamily="{StaticResource font}" MinWidth="368" Foreground="White" Panel.ZIndex="1" MinHeight="274.04" Margin="10,10,10,10">TEST
            <TextBlock.Effect>
                <DropShadowEffect
      ShadowDepth="1.7"
      Direction="319.7"
      Color="Black"
      Opacity="1"
     BlurRadius="0" RenderingBias="Performance"/>
            </TextBlock.Effect>
        </TextBlock>
    </Grid>
</Window>
